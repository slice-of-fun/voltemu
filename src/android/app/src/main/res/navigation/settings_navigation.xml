<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settings_navigation"
    app:startDestination="@id/settingsFragment">

    <fragment
        android:id="@+id/settingsFragment"
        android:name="dev.volt_emu.volt.features.settings.ui.SettingsFragment"
        android:label="SettingsFragment">
        <argument
            android:name="menuTag"
            app:argType="dev.volt_emu.volt.features.settings.model.Settings$MenuTag" />
        <argument
            android:name="game"
            app:argType="dev.volt_emu.volt.model.Game"
            app:nullable="true" />
        <action
            android:id="@+id/action_settingsFragment_to_settingsSearchFragment"
            app:destination="@id/settingsSearchFragment" />
    </fragment>

    <action
        android:id="@+id/action_global_settingsFragment"
        app:destination="@id/settingsFragment" />

    <fragment
        android:id="@+id/settingsSearchFragment"
        android:name="dev.volt_emu.volt.features.settings.ui.SettingsSearchFragment"
        android:label="SettingsSearchFragment" />

    <fragment
        android:id="@+id/freedrenoSettingsFragment"
        android:name="dev.volt_emu.volt.fragments.FreedrenoSettingsFragment"
        android:label="@string/freedreno_settings_title">
        <argument
            android:name="game"
            app:argType="dev.volt_emu.volt.model.Game"
            app:nullable="true"
            android:defaultValue="@null" />
    </fragment>

    <action
        android:id="@+id/action_settingsFragment_to_freedrenoSettingsFragment"
        app:destination="@id/freedrenoSettingsFragment" />

</navigation>
